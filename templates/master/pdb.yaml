{{- if and (eq .Values.architecture "sentinel") .Values.master.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "valkey.fullname" . }}-master
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "valkey.master.labels" . | nindent 4 }}
  {{- with (include "valkey.annotations" .) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.master.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.master.podDisruptionBudget.minAvailable }}
  {{- end }}
  {{- if .Values.master.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.master.podDisruptionBudget.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "valkey.master.selectorLabels" . | nindent 6 }}
{{- end }}